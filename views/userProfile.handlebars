    <div class="container">
        <br>
        <div class="row">
            <div class="col s4">
                <img src={{user.image}} />
            </div>
            <div class="col s8">
                <h2>{{user.first_name}} {{user.last_name}}</span></h2>
                <p>{{user.email}}</p>
                <p>{{user.phone}}</p>
                <a class="waves-effect waves-light btn-large"><i class="material-icons left" href="/add-pet">mode_edit</i>Add a Pet</a>
            </div>
        </div>
        <div class="divider"></div>
        <br>
        <div id="display-pets">
            
        </div>
    </div>